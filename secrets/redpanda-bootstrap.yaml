# Redpanda cluster bootstrap config — applied on first start.
# See: https://docs.redpanda.com/current/manage/cluster-maintenance/cluster-property-file/
enable_sasl: true
superusers:
  - admin
kafka_enable_authorization: true
sasl_mechanisms:
  - SCRAM
